---
import Layout from '~/layouts/main.astro';

const { frontmatter } = Astro.props;
---

<Layout content={frontmatter}>
  <section class="p-6 lg:p-12 mx-auto max-w-7xl">
    <h1 class="font-bold font-heading text-4xl md:text-5xl">{frontmatter.title}</h1>
    <slot />
  </section>
</Layout>
